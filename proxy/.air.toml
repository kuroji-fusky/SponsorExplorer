#:schema https://json.schemastore.org/any.json
root = "."
tmp_dir = "tmp"

[build]
exclude = ["tmp", "vendor", "cmd", "gocache"]

[color]
main = "yellow"
watcher = "cyan"
build = "green"
log = "white"
