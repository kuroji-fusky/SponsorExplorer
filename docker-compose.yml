services:
  proxy-cache:
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - "redis_cache:/cache"
    command: ["redis-server"]
    deploy:
      resources:
        limits:
          memory: 128M

volumes:
  redis_cache:
