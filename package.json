{
  "name": "sponsorexplorer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm --filter se-next-client dev",
    "build": "pnpm --filter se-next-client build",
    "preview": "pnpm --filter se-next-client preview",
    "lint": "biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "only-allow": "^1.2.1"
  },
  "packageManager": "pnpm@9.15.0"
}
